expose:
  type: nodePort
  tls:
    enabled: false
  nodePort:
    ports:
      http:
        nodePort: 30002

externalURL: http://localhost:30002

persistence:
  enabled: true

imagePullPolicy: IfNotPresent
